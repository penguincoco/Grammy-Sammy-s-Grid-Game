using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WithChanges : MonoBehaviour {

	/*
	 * Basically just a random script I put in here so I can tell when I'm working in the actual clone
	 * vs. the version with changes because I'm starting to get really confused and don't know an
	 * easier way to tell quickly :/ 
	 */
	
	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
